services:
  family-e-ink-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: family-e-ink-dashboard
    restart: unless-stopped
    ports:
      - "5002:5000"
    environment:
      - LOG_LEVEL=DEBUG
      # - ICS_URL=moved to env file
    env_file:
      - .env
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
